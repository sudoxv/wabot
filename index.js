import { default: makeWASocket } from 'baileys';

const startClient = async () => {
    try {
        const Options = {
        }
    } catch (e) {
        console.error('Terjadi kesalahan pada saat startClient:', e);
    }
}